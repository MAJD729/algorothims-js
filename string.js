// pseudo code:
// Define a variable X
// make a for loop that loop through X
// if statement that exchange nigative elemnts to string


// code:

var X = [1,-4,0,-1]

for(var i=0; i< X.length ; i++ ){
    if(X[i]<0){
        X[i]= "Turning"
    }
}